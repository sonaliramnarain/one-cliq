    <div>
      <div class="container">
         <div cover mb-4 d-flex align-items-center justify-content-center>

         </div>
          <div class="d-flex row justify-content-center">
            <div class="col-lg-4">
               <div class="card bg-light my-3">
                    <div class="card-body d-flex align-items-center">
                      <h2 class="card-title fw-bolder"> <%= @event.name %></h2>
                        </div>
                          <ul class="list-group list-group-flush">
                           <li class="list-group-item">
                           <p><strong>class: </strong><%= @agenda.classroom%></p>
                           <p><strong>Starts at: </strong><%= @agenda.start_time%></p>
                           <p><strong>Ends at: </strong><%= @agenda.end_time%></p>


                           </li>

                          </ul>
                                <%= link_to "update", edit_event_path(@event), class: 'text-center', class: 'btn btn-gradient m-4'%>

                                <%= link_to "Delete",event_path(@event),
                                method: :delete,
                                data: { confirm: "Are you sure?" },class: 'text-center', class: 'btn btn-ghost m-4' %>
                </div>
                  <div class= "d-flex align-items-center justify-content-center mb-4">
        <%= link_to 'Back', events_path(@event), class: 'text-center', class: 'btn btn-gradient m-4' %>
           </div>
            </div>
          </div>


      </div>
    </div>